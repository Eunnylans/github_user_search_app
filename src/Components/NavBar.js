import React from "react";
import "./NavBar.css";

const NavBar = () => {
  return (
      <nav className="navbar bg-danger app-header">
        <div className="container-fluid">
          <span className="navbar-brand mb-0 h1 app-title">GusApp</span>
        </div>
      </nav>
  );
};

export default NavBar;
